# Copy to .env and adjust for local development. .env is gitignored — never
# commit real credentials. These values feed compose.yaml.
POSTGRES_USER=exam
POSTGRES_PASSWORD=exam
POSTGRES_DB=practice_exam
POSTGRES_PORT=5432

# Connection string for tooling (Sqitch pg target / Npgsql), for reference:
#   db:pg://exam:exam@localhost:5432/practice_exam
